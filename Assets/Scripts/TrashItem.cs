using UnityEngine;

/// <summary>
/// Marker component for ocean-washed trash (the game's enemies). Attach this to
/// every trash prefab so systems like TurtleNest can identify hazardous contact
/// by component instead of by tag string.
/// </summary>
public class TrashItem : MonoBehaviour
{
}
